import 'package:flutter/material.dart';

class PreviousSessionScreen extends StatelessWidget {
  static const String id = "prev_session_screen";
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
